import { ResultAsync } from "@dendronhq/common-all";
import { Database } from "sqlite3";
import { SqliteError } from "../SqliteError";
export declare class VaultNotesTableRow {
    vaultId: number;
    noteId: string;
    constructor(vaultId: number, noteId: string);
}
/**
 * Utilities for read/write on the VaultNotes Table in sqlite
 */
export declare class VaultNotesTableUtils {
    static createTable(db: Database): ResultAsync<null, SqliteError>;
    static getVaultFsPathForNoteId(db: Database, noteId: string): ResultAsync<string, SqliteError>;
    static insert(db: Database, row: VaultNotesTableRow): ResultAsync<null, SqliteError>;
    static bulkInsert(db: Database, rows: VaultNotesTableRow[]): ResultAsync<null, SqliteError>;
    static delete(db: Database, row: VaultNotesTableRow): ResultAsync<null, SqliteError>;
}
