import { ResultAsync } from "@dendronhq/common-all";
import { Database } from "sqlite3";
import { SqliteError } from "../SqliteError";
export declare class VaultsTableRow {
    id: number;
    name: string;
    fsPath: string;
    constructor(id: number, name: string, fsPath: string);
}
export declare class VaultsTableUtils {
    static createTable(db: Database): ResultAsync<null, SqliteError>;
    static getIdByFsPath(db: Database, fsPath: string): ResultAsync<number, SqliteError>;
    static insert(db: Database, row: Omit<VaultsTableRow, "id">): ResultAsync<null, SqliteError>;
}
