import { ResultAsync } from "@dendronhq/common-all";
import { Database } from "sqlite3";
import { SqliteError } from "../SqliteError";
export declare class SchemaNotesTableRow {
    noteId: string;
    moduleId: string;
    schemaId: string;
    constructor(noteId: string, moduleId: string, schemaId: string);
}
export declare class SchemaNotesTableUtils {
    static createTable(db: Database): ResultAsync<null, SqliteError>;
    static insert(db: Database, row: SchemaNotesTableRow): ResultAsync<null, SqliteError>;
    static truncate(db: Database): ResultAsync<null, SqliteError>;
    static getByNoteId(db: Database, key: string): ResultAsync<any, SqliteError>;
}
