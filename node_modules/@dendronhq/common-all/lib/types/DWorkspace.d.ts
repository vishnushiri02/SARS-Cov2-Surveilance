import { DVault } from "./DVault";
import { RemoteEndpoint } from "./RemoteEndpoint";
export declare type DWorkspace = {
    name: string;
    vaults: DVault[];
    remote: RemoteEndpoint;
};
export declare type DWorkspaceEntry = Omit<DWorkspace, "name" | "vaults">;
